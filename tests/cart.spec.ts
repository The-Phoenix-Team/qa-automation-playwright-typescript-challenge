import { test, expect } from "@playwright/test";
import { LoginPage } from "../pages/loginPage";

// test("Some test after login", async ({ loginPage }) => {
//     // No need to login again, it's handled by the fixture!
// });
